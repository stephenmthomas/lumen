<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ═══════════════════════════════════════════════════════════════
         TABPAGECONTROL STYLE — Themed
         
         Tab pages only. Works by hiding the tab selectors and sending them
         to the 0 dimension.
         
         Usage:
           <TabControl Style="{StaticResource TabPageControlStyle}">
               <TabItem Header="Page1"> ... </TabItem>
               <TabItem Header="Page2"> ... </TabItem>
           </TabControl>
         ═══════════════════════════════════════════════════════════════ -->

    <Style x:Key="TabPageControlStyle" TargetType="{x:Type TabControl}">
        <Setter Property="Background"      Value="Transparent"/>
        <Setter Property="BorderBrush"      Value="{DynamicResource Theme.Brush.Border.Separator}"/>
        <Setter Property="BorderThickness"  Value="0"/>
        <Setter Property="Padding"          Value="0"/>
        <Setter Property="FontFamily"       Value="{DynamicResource Theme.Font.Family}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Grid SnapsToDevicePixels="True"
                          KeyboardNavigation.TabNavigation="Local">

                        <!-- Tab strip — collapsed but still in the tree so
                             TabControl internals work normally -->
                        <TabPanel x:Name="HeaderPanel" IsItemsHost="True" Height="0" Width="0" Visibility="Collapsed"/>

                        <!-- Content / Page -->
                        <Border x:Name="ContentPanel" Background="Transparent" BorderThickness="0" KeyboardNavigation.TabNavigation="Local" KeyboardNavigation.DirectionalNavigation="Contained" KeyboardNavigation.TabIndex="1">
                            <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="True"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
